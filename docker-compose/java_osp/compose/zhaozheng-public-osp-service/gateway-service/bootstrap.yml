spring:
  profiles:
    active: prod
  application:
    name: gateway-service
  cloud:
    config:
      name: gateway-service
      profile: prod
      uri: http://zhaozheng-public-osp-config-service:8763
